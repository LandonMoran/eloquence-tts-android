/* Mandarin Chinese code points, hand-stubbed: the Apple module's
   characters are covered by the Western list the engine already knows
   (see oracle/lpta-remake.md). */

#include <stdint.h>

#include "delta.h"

const delta_codepoint chs_delta_codepoints[] = {
    { 0, 0 },
};

const int32_t chs_delta_codepoints_n = 0;